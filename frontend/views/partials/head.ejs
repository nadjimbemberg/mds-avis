<!DOCTYPE html>
<html lang="fr" class="h-full bg-gray-900">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= typeof title !== 'undefined' ? title + ' — MDS Avis' : 'MDS Avis' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = { theme: { extend: {} } }
    </script>
    <style>
        [x-cloak] { display: none !important; }
        .star-btn { cursor: pointer; transition: color 0.15s; }
    </style>
</head>
<body class="h-full bg-gray-900 text-white antialiased flex flex-col min-h-screen">
